@model IEnumerable<RestaurantReviews.Models.RestaurantListViewModel>

@{
    ViewBag.Title = "Home Page";
}

<form method="get" action="@Url.Action("Index")" data-ismat-ajax="true" data-ismat-target="#restaurantList">
    <input type="search" name="searchTerm" data-ismat-autocomplete="@Url.Action("Autocomplete")"/>
    <input type="submit" value="Search By Name" />
</form>

@*@using(Ajax.BeginForm(
    new AjaxOptions
    {
        HttpMethod="get",
        InsertionMode=InsertionMode.Replace,
        UpdateTargetId="restaurantList"
    }))
{
    <input type="search" name="searchTerm" data-ismat-autocomplete="@Url.Action("Autocomplete")" />
    <input type="submit" value="Search By Name" />
}*@

@Html.Partial("_Restaurants", Model)
